# Data policy

`sample_inputs/` may contain a few small, redistributable images. `custom_inputs/` contains immutable user-captured inputs. Downloaded datasets belong in ignored `data/datasets/`. Never rewrite originals in place; preprocessing outputs belong under the corresponding run.
