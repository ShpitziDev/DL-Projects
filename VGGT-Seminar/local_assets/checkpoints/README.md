# Local checkpoints

Large checkpoint files are ignored. `checkpoint_manifest.json` records the exact official artifact used. Phase 3 uses only `model.pt` from `facebook/VGGT-1B`; do not add the duplicate safetensors representation.
