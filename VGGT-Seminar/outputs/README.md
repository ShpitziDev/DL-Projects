# Generated outputs

Outputs are not source artifacts. Every experiment will use a unique directory and include its resolved configuration, environment metadata, input manifest, predictions, metrics, timings, and observations. The directory skeleton is retained with `.gitkeep` files; generated contents are ignored.
